<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.meeting.mapper.NoticeMapper">
    <resultMap id="BaseResultMap" type="com.meeting.model.Notice" >
        <id property="id" column="id" jdbcType="INTEGER" />
        <result property="content" column="content" jdbcType="VARCHAR" />
        <result property="type" column="type" jdbcType="VARCHAR" />
        <result property="writer" column="writer" jdbcType="VARCHAR" />
        <result property="create_at" column="create_at" jdbcType="VARCHAR" />

    </resultMap>
    <select id="getAllNo" resultMap="BaseResultMap">
        select * from notice
    </select>
    <insert id="addNotice">
        insert into notice (content,type,writer,create_at) values (#{content},#{type},#{writer},#{create_at})
    </insert>
    <update id="updateNotice">
        update notice set content=#{content},type=#{type},writer=#{writer},create_at=#{create_at} where id=#{id}
    </update>
    <delete id="deleteNotice">
        delete from notice where id=#{id}
    </delete>
</mapper>