<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.meeting.mapper.LeaveMapper" >
    <resultMap id="BaseResultMap" type="com.meeting.model.Leave" >
        <id property="id" column="id" jdbcType="INTEGER" />
        <result property="user_id" column="user_id" jdbcType="INTEGER" />
        <result property="meeting_id" column="meeting_id" jdbcType="INTEGER" />
        <result property="reason" column="reason" jdbcType="VARCHAR" />
        <result property="create_at" column="create_at" jdbcType="VARCHAR" />
        <result property="status" column="status" jdbcType="INTEGER" />
    </resultMap>


    <select id="queryAll" resultType="java.util.Map">
        select leavenote.id,`user`.username,meeting.`name`,leavenote.reason,create_at,leavenote.status from leavenote,user,meeting,meetingpeople
        where `user`.id = leavenote.user_id and meetingpeople.user_id = leavenote.user_id and meetingpeople.meeting_id = meeting.id and leavenote.user_id=#{user_id}
    </select>

    <insert id="addLeave" parameterType="map">
        insert into leavenote (user_id,start_time,end_time,reason) values (#{user_id},#{start_time},#{endtime},#{reason})
    </insert>

    <update id="checkLeave" parameterType="int">
        update leavenote set status = #{status} where id = #{id}
    </update>

    <select id="showAll" resultType="java.util.Map">
        select leavenote.id,`user`.username,meeting.`name`,leavenote.reason,create_at,leavenote.status from leavenote,user,meeting,meetingpeople
        where `user`.id = leavenote.user_id and meetingpeople.user_id = leavenote.user_id and meetingpeople.meeting_id = meeting.id
    </select>






</mapper>
